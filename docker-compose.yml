version: '3'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: frontend/dev.Dockerfile
    command: npm run both
#    command: http-server dist --port 80
    ports:
      - "80:80"